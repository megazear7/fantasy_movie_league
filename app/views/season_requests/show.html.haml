%p
  = @season_request.requester.name
  is asking 
  = @season_request.requestee.name
  to join the
  = @season_request.season.name
  season
%p
  %div
    = @season_request.requester.name
    says:
  = @season_request.message
= link_to 'Accept', accept_season_request_path(@season_request), class: "btn-link"
= link_to 'Ignore', @season_request, :method => :delete, :data => { :confirm => 'Are you sure?' }, class: "btn-link"
= link_to 'Back', season_requests_path, class: "btn-link"
